[tools]
python = "3.14.5"
uv = "0.11.14"

[env]
_.python.venv = { path = ".venv", create = false }
# Make uv use mise's python instead of its own bundled one
UV_PYTHON_PREFERENCE = "only-system"
UV_PYTHON_DOWNLOADS = "never"
